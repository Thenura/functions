import { ExtensionRegistration } from '../dev/create-extension.js';
import { IdentifiersExtensions } from '../../models/app/identifiers.js';
import { Extension } from '../../models/app/extensions.js';
import { Result } from '@shopify/cli-kit/common/result';
export interface MatchResult {
    identifiers: IdentifiersExtensions;
    pendingConfirmation: {
        extension: Extension;
        registration: ExtensionRegistration;
    }[];
    toCreate: Extension[];
    toManualMatch: {
        local: Extension[];
        remote: ExtensionRegistration[];
    };
}
export declare function automaticMatchmaking(localExtensions: Extension[], remoteRegistrations: ExtensionRegistration[], identifiers: {
    [localIdentifier: string]: string;
}): Promise<Result<MatchResult, Error>>;
